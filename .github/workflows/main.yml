name: test
on: 
  workflow_dispatch:
    inputs:
      conf:
        description: "true or false"
        required: true
        type: boolean

jobs:
  test:
    uses: ./.github/workflows/test.yml
    if: ${{ github.event.inputs.conf == 'true' }}
